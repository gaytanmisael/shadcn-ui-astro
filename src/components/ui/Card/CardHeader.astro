---
interface Props {
  class?: string
  [x: string]: any
}

const { class: className, ...rest } = Astro.props as Props
---

<div
  class:list={["flex", "flex-col", "space-y-1.5", "p-6", className]}
  {...rest}>
  <slot />
</div>
