---
interface Props {
  class?: string
  [x: string]: any
}
const { class: className, ...rest } = Astro.props as Props
---

<div class:list={["flex", "items-center", "p-6", "pt-0", className]} {...rest}>
  <slot />
</div>
