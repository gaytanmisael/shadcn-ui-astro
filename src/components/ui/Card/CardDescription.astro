---
interface Props {
  class?: string
  [x: string]: any
}

const { class: className, ...rest } = Astro.props as Props
---

<p class:list={["text-sm", "text-muted-foreground", className]} {...rest}>
  <slot />
</p>
