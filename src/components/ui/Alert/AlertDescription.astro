---
import type { HTMLAttributes } from "astro/types"
type Props = HTMLAttributes<"p">
const { class: className, ...attrs } = Astro.props as Props
---

<div class:list={["text-sm", "[&_p:leading-relaxed]", className]} {...attrs}>
  <slot />
</div>
