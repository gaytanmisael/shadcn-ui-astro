---
import type { HTMLAttributes } from "astro/types"

type Props = HTMLAttributes<"tbody">

const { class: className, ...attrs } = Astro.props as Props
---

<tbody class:list={["[&_tr:last-child]:border-0", className]} {...attrs}
  ><slot /></tbody
>
