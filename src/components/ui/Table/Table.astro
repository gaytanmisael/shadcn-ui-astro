---
interface Props {
  class?: string
  [x: string]: any
}

const { class: className, ...rest } = Astro.props as Props
---

<div class="relative w-full overflow-auto">
  <table
    class:list={["w-full", "caption-bottom", "text-sm", className]}
    {...rest}>
    <slot />
  </table>
</div>
